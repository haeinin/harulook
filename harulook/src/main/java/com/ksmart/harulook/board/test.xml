<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
 
<mapper namespace="com.ksmart.harulook.board.test">
    
    <select id="getBoardCount" resultType="int">
        SELECT COUNT(*) FROM sns_board
    </select>

</mapper>

